services:
  - type: web
    name: awaaz360-backend
    runtime: node
    plan: free
    rootDir: awaaz360-react
    buildCommand: npm install
    startCommand: node backend/server.js
    envVars:
      - key: NODE_ENV
        value: production
